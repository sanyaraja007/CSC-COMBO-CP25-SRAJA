
public class InClassFeb10 {{
int numGrad = 0;
int numGrade = 0;
if(numGrad >=60 && numGrade < 70)
{
	
 String letterGrade = "D";
}
else if (numGrad >= 70 && numGrade < 80)
{
	String letterGrade = "C";

}

}}
